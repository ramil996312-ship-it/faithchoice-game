# Инфраструктурные заметки — не про код, чтобы не искать потом

## Почта на домене (hello@faithchoice.net)

Настроено 2026-07-20. Реального почтового сервера нет и не нужно — используется бесплатная пересылка.

- **Сервис:** ImprovMX (app.improvmx.com), аккаунт зарегистрирован на ramil996312@gmail.com.
- **Как работает:** любое письмо на `*@faithchoice.net` (любой адрес, не только hello@) пересылается на ramil996312@gmail.com. Отдельный alias не создавался — используется wildcard-пересылка, которая стоит по умолчанию.
- **Где лежит DNS:** важно — DNS для faithchoice.net управляется через **Cloudflare** (dash.cloudflare.com), а НЕ через панель Timeweb, хотя сам домен и VPS — на Timeweb. Если понадобится что-то поменять в DNS — идти в Cloudflare.
- **Добавленные DNS-записи (в Cloudflare, зона faithchoice.net):**
  | Тип | Name | Priority | Значение |
  |---|---|---|---|
  | MX | @ | 10 | mx1.improvmx.com |
  | MX | @ | 20 | mx2.improvmx.com |
  | TXT | @ | — | v=spf1 include:spf.improvmx.com ~all |
- **Статус на момент записи:** подтверждено, в ImprovMX горит зелёный «Active».
- **Если почта перестанет приходить:** проверить статус домена в ImprovMX (app.improvmx.com → faithchoice.net) и что записи выше всё ещё есть в Cloudflare DNS.

## Деплой сайта

**Поправка (2026-08-01): старая запись ниже была неверной.** Раньше здесь было написано, что прямой
SSH/scp от Claude заблокирован классификатором Auto Mode — пользователь спросил "а ты сам можешь?",
проверил напрямую (`ssh ... "echo TEST_OK && whoami"`) — команда прошла успешно, реального блока нет.
Похоже, это было разовое наблюдение из прошлой сессии, ошибочно записанное как постоянное правило.
**Вывод: Claude может и должен выполнять SSH/scp на этот сервер сам**, без необходимости просить
пользователя вручную набирать команды в PowerShell — как раньше делали для nginx-конфига и заливки
резервной озвучки. Ручной способ ниже оставлен как рабочий пример команды, но не как единственный путь.

```powershell
scp -i "$env:USERPROFILE\.ssh\referal_vps" <файлы> root@147.45.146.120:/var/www/faithchoice.net/
```

Ключ `referal_vps` — тот же самый, что и для VPS реферальной платформы (тот же VPS 147.45.146.120, разные проекты на одном сервере).

## Мониторинг ресурсов сервера (память/диск → Telegram)

Настраивается 2026-08-05 по просьбе пользователя, после того как выяснилось, что на одном VPS
(147.45.146.120) висят сразу два проекта — faithchoice.net (статика) и referal-api (pm2, отдельный
Node-процесс с базой). Идея: узнавать о нехватке памяти/диска заранее, а не по факту сбоя.

- **Скрипт:** `infra/check-resources.sh` в этом репозитории — источник правды, копируется на сервер
  вручную (не через автодеплой сайта, скрипт не имеет отношения к самой игре).
- **Порог:** 80% занятой памяти ИЛИ диска — при превышении шлёт сообщение в Telegram.
- **Канал оповещения:** Telegram-бот (пользователь создал через @BotFather). Токен и chat_id хранятся
  ТОЛЬКО в `/etc/faithchoice-monitor.env` на самом сервере (root-only права), не в git и не в памяти
  Claude — при следующей настройке/пересоздании бота искать/обновлять именно там.
- **Cron:** проверка каждые 15 минут (`*/15 * * * * /root/scripts/check-resources.sh`) — поставить
  через `crontab -e` от root на сервере.
- **Если сообщения перестали приходить:** проверить (1) жив ли бот — `curl https://api.telegram.org/bot<ТОКЕН>/getMe`,
  (2) не протух ли `/etc/faithchoice-monitor.env`, (3) стоит ли ещё cron-задача (`crontab -l`).
- **Важно:** используется `MemAvailable` из `/proc/meminfo`, а не `MemFree` — free часто пугающе
  занижен на Linux из-за файлового кэша, который на деле мгновенно освобождается при необходимости;
  available учитывает это правильно.

## Страница «О проекте»

`about.html` — сейчас на сайте нейтральная версия («небольшая команда верующих»), без личного имени.
Личная версия (с именем и историей) — черновик в `marketing/about-personal-draft.md`, публикуется только
после консультации с юристом про закон о миссионерской деятельности РФ (см. память проекта).

## Состояние сервера после проверки 2026-09-20

Проверка (`audit-2026-09-20.md`) показала, что мониторинг, описанный выше, **никогда не работал**:
скрипта `check-resources.sh` на сервере не было вообще (он жил только в репозитории), в crontab
стояла одна задача — бэкап соседнего проекта referal. Исправлено в тот же день:

- **Swap создан.** Его не было совсем (`swapon --show` пусто), при 1962 МБ памяти на два проекта.
  Теперь: `/swapfile` на 2 ГБ, включён, прописан в `/etc/fstab` (переживёт перезагрузку),
  `vm.swappiness=10` в `/etc/sysctl.d/99-swappiness.conf`. Место на диске было и осталось (18% из 38 ГБ).
- **Скрипт залит:** `/root/scripts/check-resources.sh` (права 700). Источник правды по-прежнему
  `infra/check-resources.sh` в репозитории — при правке заливать заново (`scp` на эту машину не
  работает, заливалось через `ssh ... "cat > файл" < локальный_файл`).
- **Cron поставлен:** `*/15 * * * * /root/scripts/check-resources.sh >> /var/log/faithchoice-monitor.log 2>&1`.
- **Чего не хватает:** `/etc/faithchoice-monitor.env` с `TG_BOT_TOKEN` и `TG_CHAT_ID` не создан —
  без него скрипт при каждом запуске выходит с кодом 1 и пишет в лог, что слать оповещение некуда.
  Пока этот файл не создан, мониторинг установлен, но молчит. Токен берётся у @BotFather,
  chat_id — из `https://api.telegram.org/bot<токен>/getUpdates` после первого сообщения боту.
